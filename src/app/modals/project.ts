export class Project {
    constructor(public title: string, public description: string, public link: string, public price: number, public date: string, public time: string, public id: string, public state: string) {
    }

}
